dependency:
  options:
    role-file: ../../requirements.yml  # relative to the role dir
    requirements-file: ../../requirements.yml
driver:
  name: docker
platforms:
  - name: desktop
    image: fedora:latest
    pre_build_image: true
    groups:
      - cli
      - dev
provisioner:
  playbooks:
    converge: ../../desktop.yml
