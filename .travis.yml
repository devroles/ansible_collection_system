import:
  - "./tests/travis.yml"

env:
  global:
    - COLLECTION_NAMESPACE=devroles
    - COLLECTION_NAME=system
  jobs:
    - ROLE=sudoers
    - ROLE=authorized_key
    - ROLE=disable_deltarpm

install:
  - mkdir -p ~/.ssh  # Needed by authorized_key
  - touch ~/.ssh/id_rsa.pub
