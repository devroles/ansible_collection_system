driver:
  name: docker
platforms:
  - name: jenkins_master
    image: centos:8
    privileged: true
    command: /usr/lib/systemd/systemd --system
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
