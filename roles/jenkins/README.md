Jenkins Common
--------------

A role to pre-configure the system for Jenkins use, both the driver system
and any jswarm worker systems that connect

Dependencies
------------

A system with an approrpriate user pre-configured if necessary.
